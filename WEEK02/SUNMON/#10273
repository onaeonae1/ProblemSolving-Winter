/**
 * NAME: 고대 동굴 탐사
 * LINK: https://www.acmicpc.net/problem/10273
 * DATE: 2020-01-12 
 * AUTHOR: sunmon
 * TIP:
**/

#include <iostream>
#include <vector>
#include <stack>
#include <algorithm>
using namespace std;

stack<int> best;
stack<int> route;
void dfs(int here, int value, int cost)
{
route.push(here);

for(int i=here+1; i < cave.size(); i++)
  if(cave[here][there] > 0) dfs(there, value + treasure[here], cave[here][there]);

if(value-cost > best_value) best = route;
route.pop();
}
int main()
{
   
    
}
